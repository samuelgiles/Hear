<!DOCTYPE html>
<html>
<head>
  <title>Hear</title>
  <link href='http://fonts.googleapis.com/css?family=Open+Sans:400,600' rel='stylesheet' type='text/css'>
  <meta name="viewport" content="width=device-width, initial-scale=1.0, minimum-scale=1.0, maximum-scale=1.0">
  <%= stylesheet_link_tag    "application", :media => "all" %>
  <%= javascript_include_tag "application" %>
  <%= csrf_meta_tags %>
</head>
<body data-background="<%= random_background %>">
<div id="page-wrap">
	
<% if flash[:notice] %>
    <div class="notice"><%= flash[:notice] %></div>
<% end %>
<% if flash[:alert] %>
    <div id="alert" class="notice notice-alert"><%= flash[:alert] %></div>
<% end %>
	
<header class="clearfix">
	<div class="container">
		<div class="row">
			
			<div class="col3"><%= link_to(root_url) do %><h1>HEAR</h1><% end %></div>
			
			<div class="col1 user-navigation">
				<% if current_user %>
					Logged in as <%= current_user.username %> | <%= link_to "Logout", logout_path %>
				<% else %>
					<%= link_to "Sign up", sign_up_path %> or <%= link_to "Login", session_login_path %>
				<% end %>
			</div>
			
			
		</div>
		
	</div>
</header>



<div class="main clearfix">
	<div class="container">
		<%= yield %>
	</div>
</div>
</div>
</body>
</html>
